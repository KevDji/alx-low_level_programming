#include "main.h"

/**
 * add -  two integers and returns the result
 * @a:  is a number
 * @b: is the secon nuber
 * Return: sum of a and b (c)
 */
int add(int a, int b)
{
int c;
c = a + b;
return (c);
}
